<h1>Your tasks</h1>
<ul>
  <% @tasks.each do |task| %>
  <!--  <li><%#= task.title %></li>     the hash stops the code from running -->
  <li><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>
